# 热更新

### 热更新是什么
热更新是应用在运行过程中，替换、添加、删除模块，无需刷新，能够

### 为什么热模块边界外的数据不会变

直接原因：边界外的模块没有被重新执行

分点解析：
1、模块是独立执行单元：每个模块文件被import，其顶层的代码会执行一次，产生的变量、函数、对象都存储在内存。未重新执行的模块，内存数据将永不变
2、HMR只替换“受影响且被接受”的模块
当一个模块被修改，HMR会做三件事，首先重新编译当前模块，然后按照依赖链向上游找到“accept”的模块，最后重新执行从该边界开始向下游的所有模块。

### 热更新如何实现的

### 各个框架如何支持的
